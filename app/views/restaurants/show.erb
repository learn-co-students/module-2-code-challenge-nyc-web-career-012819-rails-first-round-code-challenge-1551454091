<h1>Name: <%= @restaurant.name %></h1>

<h3>Address: <%= @restaurant.address %></h3>

<ol>
  <% @pizzas.each do |pizza| %>
    <li>
      Currently we serve: <%= pizza.name %> Pizza
      <%= link_to 'More Info', pizza_path(pizza) %>

    </li>
  <% end %>
</ol>

<%= link_to "Back to All Restaurants", restaurants_path %>
