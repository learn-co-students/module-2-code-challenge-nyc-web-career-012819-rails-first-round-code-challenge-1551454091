<h1 style='margin-top:100px;font-size:2rem'>Our official pizza directory</h1>

<br>
<%= button_to("Make pizza", new_pizza_path, :method => :get) %>
<br>
<%= button_to("Go to Restaurant Index", restaurants_path, :method => :get) %>
<br>
<% @pizzas.each do |pizza| %>
  <h1 style='font-size:1.5rem;margin-top:50px'>Pizza Name:
      <%= link_to(pizza.name, pizza_path(pizza)) %>
  </h1>
  <p>Ingredients: <%= pizza.ingredients %></p>
<% end %>
